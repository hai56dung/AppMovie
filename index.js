/**
 * @format
 */

import { AppRegistry } from 'react-native';
import App from './App';
import { name as appName } from './app.json';
import SeachText from './src/screens/seach-text';
AppRegistry.registerComponent(appName, () => SeachText);
